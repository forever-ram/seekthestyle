<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "InstagramApp" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">InstagramApp</a>
          <div class="container nav-collapse navbar-right">
            <ul class="nav">
              <%#binding.pry%>
              <% if current_user %>
                <li><%= link_to "Logout", logout_path  %></li>
              <% else %>
                <li><%= link_to "Login", authenticate_path  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <% if flash[:error].present? %>
      <div class="row alert alert-danger">
      <%=flash[:error] %>
      </div>
      <%end%>
      <div class="row">
        <div class="span1"></div>
        <div class="span10 well">
          <%= yield %>
        </div>
        <div class="span1"></div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2014</p>
      </footer>

    </div> <!-- /container -->
   at the bottom
  </body>
</html>
